# MongoDB credentials
DB_USER=excalidraw
DB_PASS=change-this-password

# Paths on your host machine
# Ensure these exist before starting
DB_VOLUME_PATH=./mongo-data
MONGO_DUMP_PATH=./mongo-dumps

# Mongo Express (database web UI)
MONGOEXPRESS_USER=admin
MONGOEXPRESS_PASS=admin
